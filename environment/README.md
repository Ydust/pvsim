# Python environment

Verified platform: 64-bit Windows. Python: 3.12.14.

- `requirements.txt`: complete dependency lock file with exact versions.
- `PACKAGE_VERSIONS.md`: principal packages, purposes and versions.
- `setup.ps1`: creates `.venv` in this directory and installs dependencies.
- `verified_environment.json`: Python and dependency version records.
- `layout.json`: source-to-runtime path mapping used by the runner.
